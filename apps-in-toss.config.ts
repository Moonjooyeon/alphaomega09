import { defineConfig } from "@apps-in-toss/web-framework/config";

export default defineConfig({
  appName: "aoreport",
  brand: {
    primaryColor: "#203f35",
  },
  permissions: [],
  webBundleDir: "dist",
});
