{
  "scripts": {
    "build": "npm run build:web && npm run build:ait",
    "build:web": "vite build",
    "build:ait": "ait build",
    "dev": "node dev-server.mjs",
    "server": "node server/index.js",
    "vite": "vite --host 127.0.0.1"
  },
  "dependencies": {
    "@apps-in-toss/web-framework": "^3.0.3",
    "express": "^5.2.1",
    "pg": "^8.23.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "vite": "^7.1.1"
  },
  "engines": {
    "node": ">=20"
  }
}
